apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deployment-model.fullname" . }}-test-connection"
  labels:
    {{- include "deployment-model.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deployment-model.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
